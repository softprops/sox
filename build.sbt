sbtPlugin := true

organization := "me.lessis"

name := "sox"

version := "0.1.0-SNAPSHOT"

//seq(soxSettings:_*)

//(SoxKeys.filter in (Compile, SoxKeys.stitch)) := sox.OnlyIn("compile")

//(SoxKeys.filter in (Compile, SoxKeys.stitch)) := sox.PrefixedWith("sox-")
